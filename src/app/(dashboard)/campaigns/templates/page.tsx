import { Layout } from "lucide-react"
import { ComingSoon } from "@/components/ui/coming-soon"

export default function CampaignTemplatesPage() {
  return (
    <ComingSoon
      title="Campaign Templates"
      description="Build reusable email and message templates. Maintain brand consistency and launch campaigns faster with a shared library."
      icon={Layout}
    />
  )
}
