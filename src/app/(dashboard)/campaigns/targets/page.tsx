import { Target } from "lucide-react"
import { ComingSoon } from "@/components/ui/coming-soon"

export default function CampaignTargetsPage() {
  return (
    <ComingSoon
      title="Campaign Targets"
      description="Define and track campaign goals. Set revenue targets, conversion benchmarks, and measure how each campaign performs."
      icon={Target}
    />
  )
}
