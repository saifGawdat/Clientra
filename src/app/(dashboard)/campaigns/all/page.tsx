import { Megaphone } from "lucide-react"
import { ComingSoon } from "@/components/ui/coming-soon"

export default function AllCampaignsPage() {
  return (
    <ComingSoon
      title="Campaigns"
      description="Design and launch multi-channel marketing campaigns. Track performance, measure ROI, and nurture leads at scale."
      icon={Megaphone}
    />
  )
}
